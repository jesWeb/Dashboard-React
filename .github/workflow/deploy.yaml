name: "deploy dashboard-2025"

on:
  push:
    branches: ["jesus"]

jobs:
    deploy-dashboard :
    runs-on: ubuntu-latest
    steps:
    - run: actions/checkout@v4
    - run: echo 'Hacemos pull del codigo'
    